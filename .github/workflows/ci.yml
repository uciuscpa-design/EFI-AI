name: CI

on:
  push:
    branches: [main, 'agent/**']
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: efi_ai
          POSTGRES_USER: efi
          POSTGRES_PASSWORD: ci-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U efi -d efi_ai"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      EFI_DATABASE_URL: postgresql+psycopg://efi:ci-password@localhost:5432/efi_ai
      EFI_PAPER_TRADING: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade pip
      - run: pip install -e '.[dev]'
      - run: alembic upgrade head
      - run: pytest -q
