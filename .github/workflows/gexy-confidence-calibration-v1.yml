name: GEXY Confidence Calibration v1

on:
  push:
    branches:
      - feature/gexy
    paths:
      - .github/workflows/gexy-confidence-calibration-v1.yml
      - .gexy-trigger/confidence-calibration-v1.txt
  workflow_dispatch:

jobs:
  confidence-calibration-v1:
    if: github.actor == 'uciuscpa-design' && github.repository == 'uciuscpa-design/EFI-AI'
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 30
    permissions:
      contents: read
    defaults:
      run:
        shell: powershell
    steps:
      - name: Fit frozen selection calibration and evaluate independent sessions
        run: |
          $repo = 'C:\Users\shannon\Documents\EFI-AI'
          if (-not (Test-Path $repo)) { throw "EFI-AI repo not found at $repo" }
          Set-Location $repo
          $env:PYTHONPATH = $repo
          git pull --ff-only origin feature/gexy
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python scripts\gexy_confidence_calibration_v1_report.py
          exit $LASTEXITCODE
